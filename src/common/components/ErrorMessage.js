import React from 'react';
import styled from 'styled-components';

const ErrorContainer = styled.div``;

const ErrorMessage = () => <ErrorContainer>ERROR</ErrorContainer>;

export default ErrorMessage;
