import React from 'react';
import styled from 'styled-components';

const LoadingContainer = styled.div``;

const LoadingMessage = () => <LoadingContainer>LOADING</LoadingContainer>;

export default LoadingMessage;
