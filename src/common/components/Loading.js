import React from 'react';
import styled from 'styled-components';

const LoadingContainer = styled.div``;

const LoadingMessage = () => <LoadingContainer>ERROR</LoadingContainer>;

export default LoadingMessage;
